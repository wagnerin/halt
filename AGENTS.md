# AGENTS.md

## Cursor Cloud specific instructions

This repository (`halt`) is currently a blank/skeleton project. As of setup, it contains only a `README.md` with no source code, dependencies, build system, or services.

- **No package manager or dependencies** are configured.
- **No lint, test, or build commands** are available.
- **No services** need to be started.

When code is added to this repository, update this section with relevant development instructions (how to install dependencies, run lint/tests/build, start services, etc.).
