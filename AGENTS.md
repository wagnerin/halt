# AGENTS.md

## Cursor Cloud specific instructions

This repository (`halt`) is currently an empty/blank project initialized with only a `README.md`. There are no source files, dependencies, build systems, services, or tests to run.

- **No dependency installation** is required.
- **No services** need to be started.
- **No lint, test, or build commands** are available yet.

When code is added to this repository, update this section with relevant development instructions (how to install dependencies, run services, lint, test, and build).
