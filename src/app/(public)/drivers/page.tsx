import { PlaceholderPage } from "@/components/feedback/placeholder-page";

export default function DriversListPage() {
  return (
    <PlaceholderPage
      title="Drivers list"
      description="Driver listing and filters are pending implementation."
    />
  );
}
